pub enum Status {
    CONNECTED,
    FAILED,
}

pub fn get_user() {
    //fetch the user from db and return
}

pub fn connect_to_database() -> Status {
    //CONNECT TO DB AND RETURN THIS
    Status::CONNECTED
}
